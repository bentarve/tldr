CREATE TABLE history(
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  pageId INTEGER NOT NULL,

  FOREIGN KEY (pageId) REFERENCES page(id) ON DELETE CASCADE
);

insert:
INSERT OR FAIL INTO history(pageId)
VALUES (?);