CREATE TABLE page (
    id INTEGER  NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    platform TEXT NOT NULL,
    markdown TEXT NOT NULL
);

CREATE INDEX pagePlatform ON page(platform);

insert:
INSERT OR FAIL INTO page(name, platform, markdown)
VALUES ?;

allPages:
SELECT * FROM page;